workflows:
  android-workflow:
    name: Android Build
    max_build_duration: 30
    environment:
      flutter: 3.29.0          # конкретная версия вместо latest
    scripts:
      - name: Get packages
        script: flutter pub get
      - name: Build APK
        script: flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
